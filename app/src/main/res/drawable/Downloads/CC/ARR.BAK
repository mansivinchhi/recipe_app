#include<stdio.h>
#include<conio.h>
void main()
{
	int a[10],i,n;
	clrscr();
	printf("Enter the no of element in arrray:");
	scanf("%d",&n);
	printf("Enter the element of array:");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0; i<n; i++)
	printf("\n %d ",a[i]);
	getch();
}